const user = req.user
const today = Date.now()
let trial = new Date(now.setDate(now.getDate() + 60))

function checkPayment() {
    if(user.payment === false && date.now < trial){

    }
}

sayHello();